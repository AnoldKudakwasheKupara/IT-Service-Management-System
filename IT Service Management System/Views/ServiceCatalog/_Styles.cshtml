<style>
    .catalog-shell { max-width: 1400px; margin: 0 auto; }
    .catalog-hero { background: linear-gradient(120deg,#0f4c81,#8f171c); color:#fff; border-radius:1rem; padding:2rem; margin-bottom:1.5rem; }
    .catalog-card { background:#fff; border:1px solid #e2e8f0; border-radius:1rem; padding:1.25rem; height:100%; box-shadow:0 1px 3px rgb(15 23 42 / .08); }
    .catalog-icon { width:3rem; height:3rem; border-radius:.8rem; background:#fdf3f3; color:#8f171c; display:flex; align-items:center; justify-content:center; font-size:1.25rem; }
    .catalog-meta { color:#64748b; font-size:.85rem; }
    .catalog-badge { display:inline-flex; align-items:center; border-radius:999px; padding:.25rem .65rem; font-size:.75rem; font-weight:600; background:#f1f5f9; color:#475569; }
    .catalog-panel { background:#fff; border:1px solid #e2e8f0; border-radius:1rem; padding:1.5rem; box-shadow:0 1px 3px rgb(15 23 42 / .06); }
    .catalog-table { width:100%; border-collapse:collapse; }
    .catalog-table th,.catalog-table td { padding:.85rem 1rem; border-bottom:1px solid #e2e8f0; vertical-align:middle; }
    .catalog-table th { color:#64748b; font-size:.75rem; text-transform:uppercase; }
    .status-AwaitingApproval { background:#fff7ed; color:#9a3412; }
    .status-Approved,.status-Fulfilled { background:#ecfdf5; color:#047857; }
    .status-InFulfillment { background:#fdf3f3; color:#7a1418; }
    .status-OnHold { background:#fefce8; color:#854d0e; }
    .status-Rejected,.status-Cancelled { background:#fef2f2; color:#b91c1c; }
</style>
